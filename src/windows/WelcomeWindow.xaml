<ui:FluentWindow
    x:Class="LiveCaptionsTranslator.WelcomeWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:LiveCaptionsTranslator"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{DynamicResource L_Welcome_Title}"
    Width="700"
    Height="490"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="NoResize"
    WindowBackdropType="Mica"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Height="27"
            ShowMaximize="False"
            ShowMinimize="False">
            <ui:TitleBar.Header>
                <ui:TextBlock Padding="15,0,0,0" VerticalAlignment="Center" Text="{DynamicResource L_Welcome_Title}" />
            </ui:TitleBar.Header>
        </ui:TitleBar>

        <StackPanel Grid.Row="1" Background="{ui:ThemeResource SystemFillColorNeutralBackgroundBrush}">
            <TextBlock
                Margin="20"
                HorizontalAlignment="Left"
                FontSize="24"
                FontWeight="Bold"
                Text="{DynamicResource L_Welcome_Heading}"
                TextAlignment="Left" />
            <ui:TextBlock
                Margin="20,10,20,10"
                TextAlignment="Left"
                TextWrapping="Wrap">
                <Run Text="{DynamicResource L_Welcome_Body_1}" />
                <Run Text="{DynamicResource L_Welcome_Body_2}" />
                <Run Text="{DynamicResource L_Welcome_Body_3}" />
                <Run Text="{DynamicResource L_Welcome_Body_4}" />
                <Run FontWeight="Bold" Text="{DynamicResource L_Welcome_Body_5}" />
                <Run Text="{DynamicResource L_Welcome_Body_6}" />
                <Run FontWeight="Bold" Text="{DynamicResource L_Welcome_Body_7}" />
                <Run Text="{DynamicResource L_Welcome_Body_8}" />
                <Run FontWeight="Bold" Text="{DynamicResource L_Welcome_Body_9}" />
                <Run Text="{DynamicResource L_Welcome_Body_10}" />
                <Run Text="{DynamicResource L_Welcome_Body_11}" />
                <Run Text="{DynamicResource L_Welcome_Body_12}" />
                <Run Text="{DynamicResource L_Welcome_Body_13}" />
                <Hyperlink NavigateUri="https://github.com/SakiRinn/LiveCaptions-Translator/wiki" RequestNavigate="Hyperlink_RequestNavigate">
                    https://github.com/SakiRinn/LiveCaptions-Translator/wiki
                </Hyperlink>
                <Run FontWeight="Bold" Text="{DynamicResource L_Welcome_Body_14}" />
                <Run Text="{DynamicResource L_Welcome_Body_15}" />
                <Run FontWeight="DemiBold" TextDecorations="Underline" Text="{DynamicResource L_Welcome_Body_16}" />
                <Run Text="{DynamicResource L_Welcome_Body_17}" />
            </ui:TextBlock>

            <ui:Button
                Margin="0,10,0,0"
                Padding="30,10,30,10"
                HorizontalAlignment="Center"
                Click="CloseButton_Click"
                Content="{DynamicResource L_Welcome_Button}" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>